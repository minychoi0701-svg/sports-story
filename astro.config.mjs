// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: GitHub 아이디를 확인한 뒤 아래 URL을 실제 'https://<아이디>.github.io'로 교체하세요.
  site: 'https://example.github.io',
  base: '/sports-story',
});
